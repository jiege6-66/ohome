import 'package:flutter/material.dart';

class AppThemeColors {
  static const primary = Color(0xFF186EEF);
  static const secondary = Color(0xFF5B97FF);

  /// 通用深色页面背景
  static const pageBackground = Color(0xFF121212);
}
